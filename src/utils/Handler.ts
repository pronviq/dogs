class Handler {
  static getHeight(initial: string): string {
    return initial + " cm";
  }

  static getWeight(initial: string): string {
    return initial + " kg";
  }
}

export default Handler;
